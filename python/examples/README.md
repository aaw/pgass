# Examples

Python rewrites of the programs in [`../../examples`](../../examples), one
script per `.lp` file, showing the same problems built with `pgass.Predicate`
classes instead of ASP-Core-2 text. Run any of them directly:

```
python3 vertex_cover.py
```
